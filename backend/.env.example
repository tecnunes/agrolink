# =============================================
# AgroLink - Backend Environment Configuration
# =============================================
# Copie este arquivo para .env e configure os valores

# MongoDB Connection
MONGO_URL="mongodb://localhost:27017"
DB_NAME="agrolink"

# CORS - Origens permitidas (separadas por vírgula)
# Em produção, especifique seu domínio: https://seudominio.com.br
CORS_ORIGINS="*"

# JWT Secret Key (gere uma chave segura em produção)
# Comando para gerar: openssl rand -hex 32
JWT_SECRET_KEY="sua-chave-secreta-aqui"

# Configurações do Servidor (opcional)
HOST="0.0.0.0"
PORT="8001"
